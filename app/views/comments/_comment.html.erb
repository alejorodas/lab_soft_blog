<%= div_for(comment) do %>
  <h3>
    <%= comment.name %> <br>
    <%= comment.email %> <br>
    said:
    <%= comment.body %> <br>
    <span class='actions'>
      <%= link_to 'Delete',  [@article, comment], :confirm => 'Are you sure?', :method => :delete %>
    </span>
  </h3>

<% end %>